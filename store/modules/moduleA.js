const state = {
    fruits: [{
        name:'apple',
        price: 10
    }, {
        name: 'orange',
        price: 5
    }]
}

// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
    state,
    namespaced: true,
    getters,
    actions,
    mutations
}
